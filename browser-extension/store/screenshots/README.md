# Screenshots da loja

Use as capturas em `docs/assets/screenshots/extension/` ao preparar a listagem da loja. Priorize:

- `popup-main.png`;
- `ai-provider-setup.png`;
- `github-analysis-modal.png`;
- `capture-public-exam.png`;
- `companion-offline.png`.

As imagens são geradas por `python scripts/capture_extension_screenshots.py` com dados fictícios e
nenhuma chave preenchida.
